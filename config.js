/**
 * PIP Vietnam Admin — Configuration
 *
 * Edit API_BASE_URL to point at your running point-in-polygon backend.
 * No trailing slash.
 */
window.PIP_CONFIG = {
  // Set by deploy step to the public Cloud Run URL (https, no trailing slash).
  API_BASE_URL: "https://pip-service-y3mxaf54sa-as.a.run.app"
};
